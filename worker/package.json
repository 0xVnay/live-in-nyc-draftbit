{
  "name": "draftbit-map-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "kv:create": "wrangler kv namespace create CACHE",
    "secret": "wrangler secret put TICKETMASTER_API_KEY"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "wrangler": "^4.0.0"
  }
}
